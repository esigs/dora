(ns com.esigs.dora.persistence.interface-test
  (:require [clojure.test :as test :refer :all]
            [com.esigs.dora.persistence.interface :as persistence]))

(deftest dummy-test
  (is (= 1 1)))
