(ns com.esigs.dora.sourcecontrol.interface-test
  (:require [clojure.test :as test :refer :all]
            [com.esigs.dora.sourcecontrol.interface :as sourcecontrol]))

(deftest dummy-test
  (is (= 1 1)))
